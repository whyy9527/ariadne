*.db
__pycache__/
*.pyc
*.pyo
.DS_Store
*.egg-info/
dist/
build/
.venv/
venv/
uv.lock

# Local Ariadne config — never commit a user's config (paths leak repo layout).
# Scope to repo root so the public examples/<sample>/ariadne.config.json files
# stay tracked.
/ariadne.config.json
awesome-mcp-servers/
GROWTH_NOTES.md
