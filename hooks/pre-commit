#!/bin/sh
# Ariadne pre-commit: runs test_semantic_hint.py (scanner + MCP tests).
#
# Enable once per clone:
#   ln -sf ../../hooks/pre-commit .git/hooks/pre-commit

set -e
cd "$(git rev-parse --show-toplevel)"
python3 tests/test_semantic_hint.py
